name: Collect Lighthouse metrics

on:
  workflow_dispatch:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v3
      - run: git status
      - run: git log
      - run: git remote -v
